# Skills estacionadas

Fora do catálogo — **não custam tokens em nenhum prompt** (`docs/rules.md` §4.6).
Reativar = mover para `.claude/skills/` na task que precisar. Mudança trivial, sem cerimônia.

| Skill | Reativar em |
|---|---|
| `design-system` | épico de design system (`docs/doc.md` §11) |
| `frontend-patterns` | primeiro épico de UI |
| `benchmark` | épico de teste de carga — depende da volumetria (`docs/doc.md` §13, lacuna #1) |
| `browser-qa` | coberto pela ferramenta de E2E que a aplicação escolher; reativar só se surgir QA exploratório manual |

Nenhuma fase de UI está aberta no `docs/roadmap.md`, e as fases 0–9 são todas de backend/contexto.
